export default class Task {
    constructor(taskID, createdAt, status, color, task) {
        this.taskID = taskID;
        this.createdAt = createdAt;
        this.status = status;
        this.color = color;
        this.status = task;
  }
}
